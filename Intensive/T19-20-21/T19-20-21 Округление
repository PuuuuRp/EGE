Округление:
В большую сторону:
    1. from math import ceil (ЛУЧШЕ ТАК)
       ceil(num / 2)
    2. num // 2 + num % 2
    3. num // 2 + 1 if num % 2 == 1 else num // 2
В меньшую сторону:
    num // 2
Математическое округление:
    round(num / 2)